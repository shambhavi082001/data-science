# program that finds hypotenuse
#p = float(input('enter the length of the first side:'))
#b = float(input('enter the lengt of the second side:'))

#h = (p**2 + b**2)**0.5
#print('perpendicular:', p)
#print('base:', b)
#print('the hypotenuse is :', h)

def hypotenuse():
    p = float(input('enter the length of the first side:'))
    b = float(input('enter the lengt of the second side:'))

    h = (p**2 + b**2)**0.5
    print('perpendicular:', p)
    print('base:', b)
    print('the hypotenuse is :', h)

#calling
hypotenuse()


